# S&P 500 Multi-Factor Risk & Return Dashboard
### ACC102 Mini Assignment — Track 4 | Xi'an Jiaotong-Liverpool University

---

## Project Overview

**Research Question**: How do macroeconomic factors (interest rates, VIX, USD) and sector dynamics interact to drive S&P 500 returns, and what are the forward-looking risk scenarios?

**Intended Audience**: Retail investors, finance students, and portfolio managers who want to understand S&P 500 dynamics beyond surface-level price charts.

**Contribution to Assessment**: 15% of overall module mark (Track 4 — Interactive Data Analysis Tool)

---

## Project Structure

```
sp500_dashboard/
├── notebooks/
│   └── 01_data_fetching_eda.ipynb   # Core analysis: data fetching, EDA, regression, Monte Carlo
├── app.py                            # Streamlit interactive dashboard
├── data/                             # Auto-generated by running the notebook
│   ├── master_data.csv               # Price + macro aligned daily data
│   ├── returns_data.csv              # Returns & rolling risk metrics
│   ├── regression_data.csv           # OLS regression inputs
│   ├── sector_betas.csv              # Sector beta results
│   └── monte_carlo_results.csv       # GBM simulation terminal distribution
├── outputs/                          # Figures and summaries
│   ├── return_distribution.png
│   └── ols_regression_summary.txt
├── requirements.txt
└── README.md
```

---

## How to Run

### Step 1: Install Dependencies
```bash
pip install -r requirements.txt
```

### Step 2: Run the Analysis Notebook
Open and run all cells in:
```
notebooks/01_data_fetching_eda.ipynb
```
This will fetch live data and generate all CSV files in `/data/`.

### Step 3: Launch the Interactive Dashboard
```bash
streamlit run app.py
```
The app runs **locally** at `http://localhost:8501`

---

## Data Sources

| Source | Series | Access Method |
|--------|--------|---------------|
| Yahoo Finance | S&P 500 (^GSPC), VIX (^VIX), 11 Sector ETFs | `yfinance` |
| FRED | 10Y Treasury Yield (DGS10), Fed Funds Rate (FEDFUNDS), USD Index (DTWEXBGS), CPI (CPIAUCSL) | `pandas-datareader` |

**Data Period**: 2015-01-01 to present (~2,500 trading days)

---

## Analytical Methods

- **Returns Engineering**: Log returns, simple returns, rolling volatility (21/63/252-day), rolling Sharpe
- **OLS Regression**: S&P 500 returns ~ VIX change + 10Y yield change + USD change (HC3 robust SE)
- **Stationarity Tests**: ADF tests on all regression inputs
- **Monte Carlo (GBM)**: 10,000 paths, 252-day horizon, calibrated to recent 1-year data
- **Risk Metrics**: Historical 90/95/99% VaR and CVaR (Expected Shortfall)
- **Sector Beta**: OLS beta and alpha for all 11 GICS sectors vs S&P 500

---

## GitHub Topics
`xjtlu` `acc102` `python` `data-analysis` `finance` `streamlit` `s&p500`
